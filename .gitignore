# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
*.tflite
*.task
.env
.env.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

CLAUDE.md
CLAUDE.track-b.md
# Internal docs stay off GitHub; published docs are opted in one at a time.
# (docs/* rather than docs/ — git won't descend into an ignored directory, so
# the negation below would never be reached.)
docs/*
!docs/ARCHITECTURE.md

# Licensed melody data — generated locally by tools/melody-extract, never committed
public/melodies/
.vercel
.superpowers/

# Local Claude Code config, skills, and worktrees — never committed
.claude/
# Supabase CLI scratch state — regenerated locally
supabase/.temp/
